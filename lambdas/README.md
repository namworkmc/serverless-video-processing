# Lambda function source code
# One directory per function, e.g.:
#   lambdas/transcode/       — ffmpeg transcoding (container image)
#   lambdas/upload-handler/  — API Gateway upload integration
# Populated during the build phase.
