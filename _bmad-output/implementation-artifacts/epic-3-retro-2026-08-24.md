---
epic: 3
date: 2026-08-24
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 3 Retrospective — Status History Surface

## Epic summary

- **Epic:** 3 — Status History Surface (FR-14, FR-15, FR-16; FR-21/FR-22 grow)
- **Diff range:** `cf4c94c^..e5e3297` (17 commits, 4 merges, all 4 merges measured, 0 binary revisions; left endpoint = parent of Story 3.1's first commit, so the range includes it). The range also carries three non-story workstreams that landed between the stories: the floci 1.7.0 binary-upload fix (epic-1 AI-1 closure, PR #18), the integration-test-plan workstream (PR #19 — T1–T10 suite, smoke-Lambda retirement), and docs fixes (PRs #15/#16).
- **Stories:** 3.1 history consumer (`cf4c94c` + review fix `1ebb298`, PR #17), 3.2 history query (`a763711` + review fix `c4bd706`, PR #20) — both merged to `main`.
- **pending_stories:** `detect-epic --epic 3` returned `story_count: 2`, `pending_stories: []` — the epic is complete per sprint-status (the epic-1/epic-2 status-lag gap did not recur; epic-2 AI-6's fix held).
- **Churn:** 52 files, +7,709 / −954 non-merge lines. Dominant additions: test suites (test_history_consumer.py +573, integration conftest +397, test_history_query.py +389), terraform/history.tf +369, and three committed review-prompt artifacts (+3,814 combined — see finding F5).

### Evidence inventory

| Evidence | State |
|---|---|
| Epic spec (epics.md Epic 3, declared Given/When/Then ACs for both stories) | present |
| Story specs (spec-3-1, spec-3-2; both `status: done`, both with `baseline_commit`, review findings all resolved) | present |
| Epic context (epic-3-context.md, corrected in-story per 3.1 review) | present |
| Diff range + git_evidence.py pre-pass | present — 17 commits, 4/4 merges measured |
| Sprint status | present — accurate (`3-1`/`3-2` done, retro key `optional`) |
| Previous retro (epic-2-retro-2026-08-21.md, accepted-with-open-items, 6 action items) | present |
| deferred-work.md | present — epic-3 entries tracked, smoke-era entries marked superseded/obsolete |
| Session logs | available (this deployment retains conversation history) |
| Live stack | Docker Desktop was down at retro start (relaunched); fresh floci + full apply in the retro worktree — behavior check ran against it (below) |

## Findings

Phase 2 ran the three code lenses of `bmad-review` **inline over the epic diff** — sub-agent delegation remains unreliable on this deployment, so the lenses ran sequentially in one session; the narrowing is recorded per the missing-evidence rule. Aggregate views were derived from the git_evidence pre-pass plus direct reads of every file the epic added or changed.

### Aggregate views

**AV-1 — Architecture delta (checked, clean).** Two new Lambdas (`history-consumer`, `history-query`), one table (`status-history`), one queue (`history-queue`), one NEW `video.processed` rule (`video-processed-to-history`), one ESM, one gateway route (`GET /videos/{videoId}/history`). All dependency edges flow through the shared layer; the spec boundary "NO new shared-layer code" was honored — the range's only `_shared/` changes are two docstring/comment edits (`clients.py` +3/−3, `tests/conftest.py` +1/−1). IAM roles match each handler's actual surface (`terraform/history.tf:140-181,266-296`). No cycles, no layering violations. `terraform state list` confirms all 18 history resources; upload-leg and trigger-leg resources untouched.

**AV-2 — Duplication map (findings F1, F2).**
- **F1 — `_parse_detail` now exists verbatim twice:** `lambdas/sfn_trigger_shim/handler.py:92-105` and `lambdas/history_consumer/handler.py:79-91` are identical copies of the EventBridge-unwrap helper. Same duplication class epic-2-retro-item-6 resolved for `_require_field`; the Story 3.1 spec's "no new shared-layer code" boundary blocked consolidation in-story. Already tracked (deferred-work.md, Story 3.1 entry). Epic 4.1's search consumer is the third copy waiting to happen.
- **F2 — hand-maintained zip `source{}` blocks grew from 3 copies to 5:** `terraform/upload.tf` (7), `terraform/processing.tf` (7), `terraform/trigger.tf` (7), `terraform/history.tf` (14 — two zips). Adding a module to `lambdas/_shared/` now requires five synchronized edits; failure surfaces only as an ImportError at live invoke (now backstopped by the integration suite, but only for journeys the suite exercises). Recurrence of epic-2 AV-2's zip-block finding; the archive-provider strategy decision (deferred-work.md, Story 1.2 entry) is still open.
- Checked, not a finding: the `_metadata_table()`/`_history_table()` module-level accessors duplicated across both new handlers are the established monkeypatch convention (mirrors the shim), not accidental duplication.

**AV-3 — Size growth (checked, clean).** Top churn is test files; handlers are 193 lines (consumer) and 106 lines (query). `terraform/history.tf` at 365 lines carries two functions' complete wiring — large but structured, not a god-file candidate.

**AV-4 — Pattern divergence (checked, clean).** Both handlers mirror the established conventions: module-level table accessors, per-record outcome tally (consumer), API GW v2 response shape + `map_error` catch-all (query), whitespace-stripped inputs (consumer `handler.py:117-121`, query `handler.py:61` — consistent with the shim strip fix from epic-2 AI-1). Config-not-code held: table names strictly from env vars.

**AV-5 — Spec-to-implementation reconciliation (findings F4, F5, F6).**
- Both stories built exactly as specified; every in-story review finding is resolved and checked off in the specs (3.1: 8 patched + 3 deferred; 3.2: 1 patched + 1 deferred). The status-validation tightening (`handler.py:131-134`), the producer→consumer wire-shape coupling test (`test_history_consumer.py:137-146`), and the empty-branch gateway pin (`tests/integration/test_history_query.py:36-52`) all landed.
- **F4 — epics.md still describes "one rule, additional target" while as-built is "separate rule per consumer."** Story 3.1 AC1: "the `video.processed` rule now targets the history queue **in addition to any existing targets**" (`epics.md:350`). As-built, history has its OWN rule `video-processed-to-history` (`terraform/history.tf:70-77`) — the 3.1 review loop fixed `epic-3-context.md:19` for exactly this contradiction but `epics.md` was not reconciled. Story 4.1's AC1 inherits the same assumption ("the `video.processed` rule now fans out to both the history and search queues"), so Epic 4 will spec against a wiring shape that doesn't exist. Spine AD-1's own wording ("new queue + new rule target") is ambiguous enough to keep breeding this.
- **F5 — three transient review-prompt artifacts committed (~3,814 lines).** `review-prompt-blind-hunter.md` (+1,274), `review-prompt-edge-case-hunter.md` (+1,270), `review-prompt-verification-gap.md` (+1,270) landed in `_bmad-output/implementation-artifacts/` via `6b2d8f6` ("integration test suite Step 1"). They are full-diff review scaffolding — prompt text + the entire diff under review — with no value after the review closed; they are not gitignored, and they were still carrying uncommitted modifications in the main checkout at retro start.
- **F6 — Bruno `Local.bru` committed with a stale concrete apiId.** The epic changed the env file from the `REPLACE_WITH_API_ID` placeholder to `6a53d768c7` (the apiId of whichever stack was applied during Story 3.2). Verified live in this retro: a fresh `terraform apply` minted `5df4735e55`, and the committed collection 404'd on all three requests until the env was hand-edited. This is a trap for SM-1's clean destroy+apply verification (Story 4.4) and contradicts FR-22's "collection passes"; `upload-video.bru:37-39` documents the replacement, but the committed state should be the placeholder.

### Diff-scope review lenses

**Adversarial:**
- **F7 — the consumer trusts the arriving `eventId`** (deferred-work.md, Story 3.1 entry): dedupe rests on eventId being UUID5(videoId, status), but a fabricated eventId paired with a KNOWN videoId is written as-is. Mitigated by AD-6's closed publisher allow-list (only event-publisher constructs `video.processed`); adding the cross-check changes frozen I/O-matrix behavior, so it stays a spec-level decision. Disposition: defer (tracked).
- Checked and clean: dedupe is the write itself (`handler.py:149-158`, `attribute_not_exists(eventId)`); poison vs transient decided purely by exception type (`handler.py:139-145`); illegal statuses rejected before the write (`handler.py:131-134`); queue visibility timeout 300s = 10× function timeout (`history.tf:41`); ESM `batch_size = 1` (`history.tf:209`); the query's 404 gate runs before any scan (`history_query/handler.py:73`); invoke permission scoped to the single route (`history.tf:332-339`).

**Edge-case hunter:**
- Checked and handled: non-dict body / non-dict detail parse / stringified detail (`handler.py:79-91,102-112`); non-string and empty required fields parametrized across `eventId`/`videoId`/`status` (3.1 review finding resolved); whitespace-padded path parameter stripped (`history_query/handler.py:61`).
- Accepted ceiling (by spec): the query's `Scan` reads one page only — no `LastEvaluatedKey` loop — and the spec explicitly never'd pagination machinery (NFR-7); the ponytail comment at `history_query/handler.py:75-77` names the GSI upgrade path. Lab scale.

**Verification gap:**
- **Epic-2's dominant gap (F7 there) is CLOSED by this epic's range:** the integration suite (T1–T10, `tests/integration/`, CI stage 5) replaced the smoke Lambda and exercises the DEPLOYED zips, handler strings, env vars, and wiring end to end — including the history leg (T8–T10) and the gateway history route. The deferred-work.md smoke entries are marked superseded with the covering test named. Verified live in this retro (below).
- **F8 — IAM scoping remains unobservable on floci** (deferred-work.md, Story 3.2 entry): least-privilege claims and the route-scoped `source_arn` apply green without enforcement. Inherited posture, not newly introduced. Defer.
- **F9 — integration T4 flaked once in this retro's live run:** `test_t4_redelivered_uploaded_event_is_no_op` failed with "no message arrived on processing-trigger-queue within 30s" on the suite's first run against the fresh stack, then passed in 18s isolation. Cold-container timing on floci vs the 30s arrival timeout (`conftest.py:171`). Not an epic-3 code defect (T4 is epic-2 territory); recorded so Epic 4's SM-1 run can watch it.
- Checked and verified live: 259 unit tests pass; 13/13 integration tests pass (after the T4 rerun); Bruno 7/7 assertions pass against the live gateway.

### Wins (evidence-confirmed)

- **The integration suite is the epic's biggest structural win:** committed, re-runnable, CI-gated coverage of the deployed stack — it retired the smoke Lambda (−470 lines of handler, −217 of smoke.tf), closed epic-2's systemic verification gap, and backstops every consumer built on this wiring from Epic 4 onward.
- **Cross-boundary coupling tests:** the producer→consumer wire-shape test (3.1) and the empty-branch-at-the-gateway pin (3.2) each guard a seam no single story's suite could see — exactly the class of defect this retro exists to find.
- **The review loop earned its keep again:** both stories patched real findings before merge (status validation, wire-shape coupling, empty-branch pin) and deferred the rest with tracked entries — zero untracked debt.
- **Sprint-status hygiene held:** epic-3's story keys were `done` when the retro started; the epic-1/epic-2 status-lag pattern (two consecutive retros' top process finding) did not recur.

## Behavior verification

Exercised live against a FRESH stack in the retro worktree (Docker Desktop was down at retro start and was relaunched; floci came up via `docker compose up -d`, full `terraform apply` — 2026-08-24):

1. **Wiring (AC1 both stories):** `terraform state list` shows all 18 history-leg resources — table, queue + policy, rule + target, consumer zip/role/policy/function, ESM, query zip/role/policy/function, integration, route, invoke permission. Upload-leg and trigger-leg resources unchanged.
2. **Unit:** `pytest lambdas/ -q` → 259 passed.
3. **Integration (deployed stack):** `pytest tests/integration/ -v` → 12/13 first run (T4 flake, see F9), 13/13 after T4 isolation rerun. History coverage: T8 upload→PROCESSED→exactly one `status-history` entry with the deterministic eventId; T9 republished `video.processed` deduped (still one entry); T10 poison event with fabricated eventId + unknown videoId dropped (no entry); gateway route tests: 200 + oracle-matched entries, known-video/zero-entries 200, unknown-video 404 passthrough.
4. **Bruno (FR-22):** first run FAILED 3/3 — committed `Local.bru` apiId `6a53d768c7` does not resolve against the fresh stack (finding F6). After pointing the env at the live apiId: 3/3 requests, 7/7 assertions pass, including the history request's poll-with-timeout chaining off the upload's captured videoId. Env file restored to committed state afterward.
5. **CI mirror:** gitleaks clean, ruff E/F clean, `terraform fmt -check` + `validate` clean.

## Previous-retro follow-through

Epic 2's retro left 6 action items; sprint-status carries all of them (ids `epic-2-retro-item-5` … `epic-2-retro-item-10`) already at `done`, and every one has landing evidence:

| Selector | Landed? | Evidence |
|---|---|---|
| `epic-2-retro-item-5-strip-asl-input-values-in-sfn-trigger-sh` | Yes | `lambdas/sfn_trigger_shim/handler.py:152` — `asl_input = {name: detail[name].strip() ...}` |
| `epic-2-retro-item-6-consolidate-duplicated-_require_field-va` | Yes | `lambdas/_shared/errors.py:38` (`require_field`) and `:51` (`is_client_error_code`); deferred-work.md Resolved 2026-08-21 |
| `epic-2-retro-item-7-add-runtime-smoke-scenarios-transcode-st` | Yes (then superseded) | Built in the 2026-08-21 batch (deferred-work.md Resolved); the scenarios were retired with `lambdas/smoke/` and their coverage is now integration tests T3–T7 — a superset, exercised live in this retro |
| `epic-2-retro-item-8-trigger-leg-readme-update-solid-diagram` | Yes | README.md trigger-leg content present (diagram, dedupe semantics, status log) |
| `epic-2-retro-item-9-decide-retry-failed-path-stance-before-e` | Yes | `README.md:231` — "Retry/FAILED-path stance (accepted lab limitation)" documented |
| `epic-2-retro-item-10-close-review-to-done-status-transition-g` | Yes | `detect-epic --epic 3` returned `pending_stories: []` with no manual frontmatter fixes needed — the gap that forced two consecutive machine-rejected verdicts is closed |

All 10 items in sprint-status `action_items` (epics 1–2) are `done`; no `--set-action-status` transition is proposed.

## Action items

All items are **proposed remediation / spec reconciliations** — the retrospective does not execute them; the human decides.

| # | Finding | Action | Owner |
|---|---|---|---|
| AI-1 | F6 (spec-to-impl, verified live) | **Restore the apiId placeholder in `bruno/environments/Local.bru`** (`REPLACE_WITH_API_ID` form, as before the epic) — the committed concrete id `6a53d768c7` 404s against any fresh `terraform apply` (proven in this retro), which will break Story 4.4's clean destroy+apply SM-1 run. Keep the replacement instruction in `upload-video.bru`. | Kygor |
| AI-2 | F4 (spec reconciliation) | **Reconcile `epics.md` rule wording to as-built AD-1** — Story 3.1 AC1's "in addition to any existing targets" (`epics.md:350`) and Story 4.1 AC1's "fans out to both queues" describe one-rule/multi-target; as-built is one rule per consumer (`video-processed-to-history`, `terraform/history.tf:70`). Fix 3.1's line as a record correction and 4.1's before the story is drafted, so Epic 4 specs against the real wiring shape. | Kygor |
| AI-3 | F5 (hygiene) | **Stop committing review-prompt artifacts** — delete or gitignore `_bmad-output/implementation-artifacts/review-prompt-*.md` (~3,814 lines of transient full-diff review scaffolding committed in `6b2d8f6`). | Kygor |
| AI-4 | F1 (duplication, recurring) | **Consolidate `_parse_detail` into the shared layer before Epic 4.1** — two verbatim copies exist (shim + history consumer); the search consumer is the third copy waiting to happen. Same pattern epic-2 AI-6 resolved for `_require_field`; deferred-work.md tracks it. | Kygor |

Deferred, tracked (no new action item): F2 zip-source-block growth (5 copies — folds into the open archive-provider packaging decision, deferred-work.md Story 1.2 entry); F7 eventId cross-check (spec-level decision, deferred-work.md); F8 IAM unobservability on floci (inherited posture, deferred-work.md); F9 T4 flake (watch at SM-1).

Process lessons (no separate action item):
- **The integration-suite investment paid off immediately:** Epic 3 is the first epic whose deployed wiring was machine-verified on every CI run, and this retro's behavior check was the cheapest one yet (run the suite, don't hand-probe). Keep building consumer stories on top of it rather than re-probing ad hoc.
- **"No new shared-layer code" story boundaries manufacture duplication:** the boundary is sound for story focus, but it has now twice deferred a consolidation that the NEXT epic's consumer immediately re-duplicates (F1). When a story spec carries that boundary, the retro candidate should become a pre-Epic-4 cleanup by default.
- **Committed environment values rot:** F6 is the second epic in a row where a value that is stack-instance-specific (first the floci version pin, now the apiId) got committed as a literal. Rule of thumb: anything that changes on `terraform destroy`+`apply` stays a placeholder in the repo.

## Acceptance verdict

**Verdict: accepted-with-open-items** (machine verdict; no human override needed — `pending_stories` was empty).

**Criteria: declared** (epics.md carries explicit Given/When/Then ACs for both stories).

**Criteria assessment:**
- **Story 3.1** — all 18 wiring resources exist after a fresh apply, existing rules untouched (state list, this retro); upload→PROCESSED produced exactly one `status-history` entry with the deterministic eventId (T8, live); redelivered event deduped (T9, live); poison event dropped, never written (T10, live); transient-error retry semantics covered by unit matrix + exception-type design; full CI mirror green. All ACs met.
- **Story 3.2** — route exists alongside the upload route with integration + route-scoped invoke permission (state list); gateway GET returns 200 with oracle-matched entries (live integration + Bruno); unknown videoId → 404 `{"error": ...}` passed through unchanged (live); Bruno collection passes against the gateway URL only with poll-with-timeout (live, 7/7 assertions — after the env apiId fix that AI-1 tracks); CI mirror green. All ACs met.

**Open items keeping this from a bare "accepted":** AI-1 (committed stale apiId — the collection as committed does not pass against a fresh stack, which FR-22 and SM-1 will judge), AI-2 (epics.md/4.1 spec divergence from as-built routing), AI-3 (review-artifact hygiene), AI-4 (deferred `_parse_detail` consolidation now one consumer away from a third copy).

## Open questions

- **Packaging decision timing (F2 + deferred archive-provider entry):** five hand-maintained zip copies now; Epic 4 adds two more functions. Does the packaging strategy (generated zip / external source / local-exec) land before Epic 4, or does the integration suite's ImportError backstop count as sufficient mitigation for the lab?
