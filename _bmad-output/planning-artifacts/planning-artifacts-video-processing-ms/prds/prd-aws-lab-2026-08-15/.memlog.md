---
topic: Video processing platform
updated: 2026-08-15T18:30
---

- (decision) Vision: personal learning lab to practice and then teach AWS + Spring microservices patterns; bar for done is all 5 services run on ministack and one upload flows end-to-end
- (decision) PRD structure: capability-first entry point (vision -> target user -> journey -> features grouped by service, each service explicitly demonstrates a named pattern) -> non-goals -> MVP scope -> success metrics
- (decision) Test surface is Bruno (REST + gRPC), not curl/grpcurl; PRD and future verification commands reference Bruno collections
- (decision) Feature scope per service demonstrates a named pattern: video-common=versioned contract (AD-5), upload=stateless ingress worker+identity (AD-1/2), processing=idempotent SQS consumer+sole terminal producer (AD-3/4), metadata=single-writer source of truth+enforced state machine (AD-2/3), notification=derived event-sourced store+SSE history (AD-2/4), search=derived event-sourced index+rebuild, title-substring (AD-2/4/8)
- (decision) FAILED path producing is OUT of scope (state machine rules kept as requirements, actual FAILED-producing exercise deferred); SSE live streaming out of demonstrated scope, history query only; search = title-substring only. Lab bar = happy path PROCESSED end-to-end
- (decision) Add AWS API Gateway v2 (HTTP API) as the single client ingress in front of the services; Bruno calls the gateway only, never services directly; all client HTTP (upload, status history, search) goes through it; gRPC stays strictly internal service-to-service; NO auth in the learning lab. Ministack emulates API Gateway v2 with HTTP_PROXY integrations + path-based data plane. PRD updated now; spine revision (deployment envelope/structural seed/dependency diagram) flagged as the follow-up step after PRD finalizes
- (change) PRD updated with API Gateway feature cluster (4.7, FR-21..FR-24: single ingress, route client HTTP, gRPC stays internal, no auth); UJ-1, glossary, MVP scope, SM-1, non-goals, assumptions, open question #2 all updated to gateway-aware
- (decision) Infrastructure on ministack MUST be provisioned with Terraform (IaC), NOT aws cli — covers S3 buckets, SQS queues, and the API Gateway v2; becomes a dedicated requirement (FR-25/26), resolves open question #2 on provisioning approach
- (change) PRD updated: new feature cluster 4.8 Infrastructure Provisioning (Terraform) with FR-25 (all ministack infra via Terraform) and FR-26 (no aws cli); MVP scope, non-goals, assumptions, SM-1 updated; open question #2 removed as resolved
- (event) Finalize step 1 (memlog audit) complete: all 9 entries captured in PRD or addendum; created addendum.md with options-considered (gateway v1 vs v2, Terraform vs cli) + ministack technical facts + known spine drift note
- (change) Finalize reviewer fixes: FR-19 restated (REST-through-gateway only, gRPC contradiction removed); FR-21 added carve-out for Bruno direct-gRPC debugging surface; FR-20 added rebuild triggers (startup + explicit endpoint); FR-16/FR-18 added AD-8 metadata validation (poison-drop) consequences; FR-5/FR-6/FR-12 thresholds tightened; FR-14 uses VideoStatus; assumptions ID'd A-1..A-6; addendum wording nit fixed (structural seed not dependency diagram); reconcile found zero contradictions (spine behind PRD, known)
- (change) Finalize polish applied: consolidated poison-event rule into FR-16 (shared derived-service rule, cross-referenced from FR-18); glossary explains dot-event vs dash-queue naming + Status history term; SM-1 widened to FR-1..FR-26; 4.7 description parentheticals fixed to -service names; 'status history' naming standardized; NOTE FOR PM callout added in 0 for gated spine features; FR-10 references FR-14 transitions
- (event) PRD finalized
- (change) PRD reconciled against the revised spine (update run, fast path): 0 NOTE FOR PM re-scoped from 'gated' to 'bound' (AD-9/AD-10, spine wins on conflict); glossary API Gateway entry cites AD-9/AD-10; FR-3 names authority moved to Terraform (Names mirrors, verification asserts terraform output, rename = coordinated change); FR-20 rebuild trigger narrowed to gRPC/admin only (no HTTP surface, spine AD-9); FR-22 routes reference the spine authoritative path table (route path == facade path, no rewriting); A-2/A-3 updated (stage dev + terraform output handoff; host.docker.internal addressing).
- (change) Addendum reconciled: Known Drift section rewritten as 'resolved 2026-08-15' with the spine decisions that now govern (enumerated client surface, path table, payload-format 1.0, gRPC/admin rebuild, stage dev, bring-up order); stage name drift fixed ( -> dev via terraform output, ms-custom-id downgraded to a note); client routes note route-path==facade-path.
- (change) Reviewer fixes applied: FR-15 scoped to client-facing (AD-9 enumerated) HTTP facades; glossary + 4.7 description qualify the direct-call ban as HTTP-only (gRPC carve-out preserved); FR-3 header cites AD-6 (names authority) not AD-10; FR-25 + addendum add bring-up order and restart-after-destroy+re-apply (AD-10); addendum stage note re-pointed (ms-custom-id optional, stage dev), section renamed 'Spine Binding Summary'.
- (event) PRD update finalized (2026-08-15): reconciled to revised spine (AD-9/AD-10/AD-6). Reviewer gate passed; lint n/a (markdown); status already final.
- (decision) Database per record store — PostgreSQL everywhere (spine AD-11), architecture-team discussion: user picked Option A (PostgreSQL for metadata, notification, search) with **Aurora PostgreSQL-compatible on real AWS** (user preferred Aurora over regular RDS). Dev = one `postgres` container, one DB per service (`metadata`, `notification`, `search`); search = Postgres `ILIKE` title-substring. Options-considered (A vs Postgres+OpenSearch vs DynamoDB+OpenSearch) documented in addendum.
- (change) PRD updated for AD-11: A-6 rewritten (H2 → PostgreSQL dev + Aurora real AWS, via `spring.datasource.*` not SCAWS); glossary "Record store" adds PostgreSQL + per-service DB + no cross-service reads; §4.4 metadata description updated (source of truth for Video record, not "only record store"); §6.1 MVP scope adds PostgreSQL record stores; addendum gains DB options-considered + record-store databases technical fact + AD-11 in Spine Binding Summary.
- (change) PRD updated for docker-ownership policy (user decision): service databases and any service-owned Docker instance live in a `docker-compose.yml` inside that service's own repository — never in the root compose (root = agentic engineering workspace: ministack + redis). A-6 rewritten accordingly (each record-owning service declares its own `postgres` in its own repo); §6.1 MVP scope updated; addendum DB options-considered + record-store databases technical fact updated. Tracks the architecture memlog decision of the same date.
