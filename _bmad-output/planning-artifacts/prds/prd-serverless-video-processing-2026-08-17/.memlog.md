---
topic: Serverless video processing platform (floci)
updated: 2026-08-17T21:35
---

- (decision) Purpose: learn AWS serverless services (replaces old lab's Spring Boot microservices JTBD); inherited artifacts are domain-reference only
- (decision) Success criterion unchanged: upload one video, watch it flow through every stage, be able to explain each architectural decision
- (assumption) Stakes = hobby/solo learning lab; PRD ~2 pages, single-operator journey, no market research needed
- (decision) Goal is purely personal learning — teaching/explaining-to-others angle dropped from vision
- (decision) PRD explicitly exercises each target AWS service (Lambda, Step Functions, DynamoDB, EventBridge, S3, API GW, SQS/SNS) as first-class learning surfaces — requirements deliberately route work through them
- (decision) Feature clusters F1-F7 confirmed as proposed
- (decision) Event schema design/versioning NOT a standalone feature — implicit in F2/F4/F5; no shared-contract cluster
- (decision) Pure event-driven serverless — no synchronous RPC/gRPC between components; all coordination via events + DynamoDB
- (decision) Transcode = zip-packaged Python Lambda with demo-mode copy fallback (time-saver); container-image Lambda with real ffmpeg deferred as future extension
- (decision) FAILED producing path deferred (same as old lab) — FAILED exists only as state-machine rules, never actually produced
- (event) Reviewer gate: rubric review GOOD (4 strong/3 adequate); all findings autofixed — testable consequences on all 24 FRs, Open Questions + Assumptions Index added (floci coverage = A-2/OQ-1), FR-19 rebuild mechanism deferred to architecture via NOTE FOR PM, FR-22 test collection added, SM-1 tightened to verify service coverage, glossary transport softened
- (event) PRD finalized
